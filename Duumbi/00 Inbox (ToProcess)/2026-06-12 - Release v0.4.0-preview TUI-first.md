---
tags:
  - duumbi/inbox/enriched
  - duumbi/status/processed
  - duumbi/classification/execution
  - duumbi/value/critical
  - duumbi/importance/high
  - duumbi/complexity/medium
duumbi_inbox_enrichment: processed
duumbi_inbox_enrichment_generated_at: 2026-06-22T19:06:37.210Z
---

# Release v0.4.0-preview (TUI-first)

<!-- duumbi-inbox-enrichment:v1 status=processed generated_at=2026-06-22T19:06:37.210Z -->

## Source
- Surface: Manual Obsidian edit
- Vault path: Duumbi/00 Inbox (ToProcess)/2026-06-12 - Release v0.4.0-preview TUI-first.md
- Submitted by: unknown unless explicit in the raw input

## Raw input
> ---
> tags:
>   - duumbi/inbox/roadmap
>   - duumbi/status/to-process
>   - duumbi/classification/execution
>   - duumbi/value/critical
>   - duumbi/importance/high
>   - duumbi/complexity/medium
> created: 2026-06-12
> milestone: M0
> source: "[[DUUMBI Future Development Roadmap Map]]"
> related_issues:
>   - hgahub/duumbi#687
> ---
> 
> # Release v0.4.0-preview (TUI-first)
> 
> ## Context
> 
> DUUMBI core works end-to-end (v0.3.3, `init → build → run`, TUI REPL), but there is **no git tag, no GitHub Release, no prebuilt binary**, and `examples/` is empty. Issue #687 already tracks "tag v0.4.0-preview with prebuilt binaries and a working install path". The TUI is the first product surface we want to ship publicly.
> 
> ## Goal
> 
> A stranger on macOS, Linux, or Windows installs DUUMBI with one documented command and reaches a working `init → build → run` plus the interactive TUI in under 10 minutes.
> 
> ## Subtasks
> 
> 1. Decide install channel: GitHub Releases binaries + install script as primary; crates.io publish optional (see [[2026-06-12 - Docs Truth Reconciliation]]).
> 2. Release CI workflow in `hgahub/duumbi`: build matrix (macOS arm64/x64, Linux x64, Windows x64 — Phase 16 already proved the MSVC target), produce signed/checksummed archives.
> 3. Tag `v0.4.0-preview`, create GitHub Release with changelog generated from phase history.
> 4. Install script (`curl | sh` style for unix, instructions for Windows) and `duumbi --version` smoke test.
> 5. Clean-machine install evidence on all three platforms (record as Stage-12-style evidence, like DUUMBI-382 conventions).
> 6. Versioning policy doc: what `-preview` means, what graduates it to v0.4.0 stable.
> 
> ## Acceptance criteria
> 
> - `gh release view v0.4.0-preview` shows binaries for 3 platforms with checksums.
> - Documented install path works on a clean VM for each platform.
> - README and docs.duumbi.dev quickstart match the released artifact exactly.
> 
> ## Links
> 
> - [[DUUMBI Future Development Roadmap Map]]
> - [[2026-06-12 - TUI as Primary Surface Polish]]
> - [[2026-06-12 - Docs Truth Reconciliation]]

## Interpreted intent

Create and publish the first public preview release of DUUMBI (v0.4.0-preview) with a working TUI, simple install path, prebuilt binaries for macOS (arm64/x64), Linux x64, and Windows x64, and matching documentation.

## Developer summary

DUUMBI core (v0.3.3) works end-to-end but has no public release artifacts. This item requests creating a v0.4.0-preview tag, GitHub Release with prebuilt binaries for three platforms, a one-command install script, and quickstart documentation that matches the released artifact exactly. The TUI is the first product surface to ship publicly.

## UML overview

```mermaid
flowchart TD
  A[Decide install channel: GitHub Releases + install script] --> B[Build release CI workflow]
  B --> C[Tag v0.4.0-preview]
  C --> D[Create GitHub Release with changelog]
  D --> E[Produce signed/checksummed binaries for macOS arm64/x64, Linux x64, Windows x64]
  E --> F[Write install script (curl | sh unix, instructions Windows)]
  F --> G[Smoke test install on clean VMs]
  G --> H[Update README and docs.duumbi.dev quickstart]
  H --> I[Draft versioning policy doc for -preview]
```

## Classification
- Type: execution
- Business value: critical
- Importance: high
- Complexity: medium

## Clarifications
### Answered
- There is no git tag, no GitHub Release, no prebuilt binary, and examples/ is empty.
- Issue hgahub/duumbi#687 already tracks this item.
- The TUI is the first product surface targeted for public release.
- Changelog should be generated from phase history.

### Open
- Should crates.io publish be included or only GitHub Releases?
- What exact version string format (e.g., v0.4.0-preview.1)?
- Is changelog format expected? (e.g., conventional commits, phase summaries)
- How should Windows install instructions differ from unix curl|sh?
- Who will own code signing and notarization for macOS and Windows binaries?

## Relevant DUUMBI context
- DUUMBI Future Development Roadmap Map — places this under milestone M0.
- 2026-06-12 - TUI as Primary Surface Polish — relates TUI readiness.
- 2026-06-12 - Docs Truth Reconciliation — ensures documentation matches release.
- DUUMBI - PRD — product thesis and user workflows for install/packaging.
- DUUMBI - Agentic Development Runbook — workflow for execution tasks.
- Source code docs/architecture.md — build pipeline context.
- Cargo.toml — current version is 0.4.1-preview, may conflict with 0.4.0-preview tag.
- src/compiler/mod.rs — Cranelift backend, relevant for platform support (Phase 16 MSVC target).
- src/graph/mod.rs — petgraph usage, important for verifying graph builds during release testing.

## Related GitHub context

Known: issue hgahub/duumbi#687 tracks this release. Triage should verify if any other related issues/PRs exist (e.g., for install script or release CI).

## Initial routing recommendation

GitHub issue

## Requested follow-up
- Create or update a GitHub issue for this release task (likely #687).
- Plan release CI workflow in .github/workflows/.
- Define install script location and behavior.
- Coordinate documentation updates in README and vault docs.
- Draft versioning policy for -preview tags.

## AI agent instructions
- The GitHub issue should be linked to hgahub/duumbi#687 or become that issue itself.
- Add subtasks as a checklist: release CI, tag, binaries, install script, smoke test, docs, versioning policy.
- Set milestone M0 and label execution.
- Reference that the install channel is GitHub Releases with a script; crates.io optional.
- Note that the Windows build target was proven in Phase 16 (MSVC).
- The issue should instruct to generate changelog from phase history.
- Include acceptance criteria: gh release view shows binaries, documented install works on clean VMs, README/docs match.

## Scope candidate
### In
- Git tag v0.4.0-preview
- GitHub Release creation with changelog
- Release CI workflow: build matrix (macOS arm64/x64, Linux x64, Windows x64)
- Prebuilt binaries with checksums/signatures
- Install script (Unix curl|sh) and Windows instructions
- Smoke test on clean VMs for all platforms
- README and docs.duumbi.dev quickstart update to match release
- Versioning policy doc for -preview

### Out
- Crates.io publish (optional, handled separately)
- Examples/ population (separate item #688)
- Studio/Desktop release (post-M0)
- API docs, full website, or marketing materials
- Backward compatibility with prior internal versions

## Risks and trade-offs
- Windows MSVC build may have hiccups not seen in Phase 16 proof-of-concept.
- Signing binaries (Apple notarization, Windows Authenticode) requires infrastructure/accounts not yet set up.
- Install script needs to be robust across different shell environments and Windows PowerShell.
- Changelog generation from phase history may miss manual notes; requires review.
- Current Cargo.toml version (0.4.1-preview) could conflict with the new tag 0.4.0-preview unless intentional.

## Obsidian tags

#duumbi/inbox/enriched #duumbi/status/processed #duumbi/classification/execution #duumbi/value/critical #duumbi/importance/high #duumbi/complexity/medium

## Enrichment result
- Date: 2026-06-22T19:06:37.210Z
- Status: ready for triage
- Canonical duplicate: none verified
- Facts:
- DUUMBI v0.3.3 core works end-to-end (init → build → run), TUI REPL exists.
- No git tag, GitHub Release, or prebuilt binary exists.
- Issue #687 already tracks this exact item.
- Phase 16 proved Windows MSVC target in CI.
- Cargo.toml version is currently 0.4.1-preview.
- Assumptions:
- The TUI is stable enough for a preview release (no major crashes or UX gaps).
- Release CI can be based on existing GitHub Actions workflows (e.g., Phase 16 build).
- Install script will follow common patterns (e.g., rustup style).
- Code signing for macOS and Windows can be set up in time or deferred to a later patch.
- The vault note 2026-06-12 - Docs Truth Reconciliation will guide documentation alignment.
- Recommendations:
- Combine this with the existing issue #687 and enrich it with the subtasks and acceptance criteria.
- Use GitHub Actions for release CI, leveraging the Phase 16 matrix.
- Defer crates.io publish to a follow-up unless explicitly requested.
- Draft the versioning policy as part of the release preparation.
- Coordinate with docs update to avoid drift between README and hosted docs.
- Perform clean-machine install evidence as a Stage-12-style evidence task (like DUUMBI-382).
